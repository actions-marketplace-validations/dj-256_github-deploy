export interface Options {
    host: string,
    username: string,
    password: string,
    privateKey: string,
    passphrase: string,
    port: number,
    local: string,
    remote: string,
    exclude: string,
    dotFiles: boolean,
    rmRemote: boolean,
}
